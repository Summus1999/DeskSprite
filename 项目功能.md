# 项目功能

## 一、大A 股票盯盘 ✅ 已实现

### 概述

右键 Live2D 角色 → 大A盯盘 → **自然语言一句话输入** → 自动解析股票和事件 → 确认盯盘 → 系统原生通知提醒。

### 交互流程

```
右键角色       设置面板                   自动解析              后台 5 分钟轮询      触发通知
  └─ 大A盯盘 ─→ ┌────────────────┐     ┌──────────────┐      ┌──────────┐      ┌──────────┐
               │ 输入一句自然语言  │     │ 股票: 贵州茅台 │      │ 交易时段  │      │ Windows  │
               │ "帮我看下贵州茅台  │ ─→  │ 事件: 涨幅≥1% │ ─→   │ 拉取行情  │ ─→   │ 原生通知  │
               │  涨幅超1%通知我"  │     │ 确认盯盘      │      │ 比对阈值  │      └──────────┘
               └────────────────┘     └──────────────┘      └──────────┘
```

### 自然语言输入示例

| 用户输入 | 解析结果 |
|----------|----------|
| 「帮我看下贵州茅台，涨幅超过1%通知我」 | 贵州茅台 (sh600519)，涨幅 ≥ 1% |
| 「盯一下宁德时代，跌破1800通知」 | 宁德时代 (sz300750)，价格 ≤ 1800 元 |
| 「关注600519，涨了3%提醒我」 | sh600519，涨幅 ≥ 3% |
| 「帮我盯茅台，涨幅超5%和跌破1500都通知」 | 贵州茅台，涨幅 ≥ 5% + 价格 ≤ 1500 |

### 解析能力

- **股票识别**：支持股票全称（贵州茅台）、简称（茅台）、6位代码（600519）
- **内置 ~80 只热门 A 股**名称映射，覆盖白酒、银行、新能源、科技、医药等板块
- **事件提取**：涨幅/跌幅百分比、价格突破/跌破阈值
- **多事件**：一句话可同时设置多个事件条件
- **模糊匹配**：输入「茅台」可匹配「贵州茅台」
- **快速补充**：解析后可一键追加 [+1%涨] [+3%涨] 等常见事件

### 已实现

| 模块 | 说明 |
|------|------|
| 数据来源 | 新浪财经 `hq.sinajs.cn` |
| 事件类型 | 涨幅阈值、跌幅阈值、价格上限、价格下限 |
| 通知方式 | Windows 原生通知（tauri-plugin-notification） |
| 交互入口 | 右键角色 → 大A盯盘 → 自然语言输入 |
| 轮询策略 | 交易时段 (9:30-11:30, 13:00-15:00) 每 5 分钟 |
| | 非交易时段停止轮询 |
| 数据持久化 | tauri-plugin-store，重启保留配置 |
| 触发机制 | 条件首次满足时通知；条件解除后自动复位 |

### 代码位置

- **前端**：`src/App.tsx`（右键菜单）、`src/components/StockPanel.tsx`（设置面板）
- **解析**：`src/utils/intentParser.ts`（NL 解析）、`src/utils/stockNames.ts`（股票名称库）
- **后端**：`src-tauri/src/stock.rs`（数据获取、轮询、事件检测、通知）
- **存储**：Store 插件管理 `monitors.json`

---

## 二、DeepSeek API 集成 ✅ 已实现

### 概述

接入 DeepSeek 大模型能力，为后续智能功能提供 AI 底座。设置成功后系统通知确认。

### 交互流程

```
右键角色              设置面板                  验证 & 存储             通知
  └─ DeepSeek API  ─→ ┌─────────────────┐     ┌──────────────┐     ┌──────────────┐
     （菜单第一项）     │ 输入 API KEY     │ ─→  │ 发送测试请求  │ ─→  │ "设置成功     │
                      │ [粘贴 Key 输入框] │     │ 验证有效性    │     │  API: sk-xx…  │
                      │                  │     │ 加密存储      │     │  模型:        │
                      │ 默认模型显示:     │     └──────────────┘     │  deepseek     │
                      │ deepseek v4pro   │                          │  v4pro"       │
                      └─────────────────┘                          └──────────────┘
```

### 右键菜单调整

```
右键角色
  ├─ DeepSeek API 设置   ← 第一项（新增）
  ├─ 大A盯盘
  ├─ ──────────────
  └─ 退出
```

### 设置面板

| 元素 | 说明 |
|------|------|
| API KEY 输入框 | 支持粘贴，输入时掩码显示（`••••••••`） |
| 默认模型 | 固定显示 `deepseek v4pro`，后续可扩展为下拉选择 |
| 测试连接按钮 | 发送一条简单请求验证 Key 是否有效 |
| 保存按钮 | 验证通过后保存到本地加密存储 |

### 通知文案

- **设置成功**：标题「DeepSeek API 设置成功」，正文「API Key: `sk-xxxx…xxxx`，默认模型: `deepseek v4pro`」
- **验证失败**：标题「DeepSeek API 验证失败」，正文「请检查 API Key 是否正确，或网络是否畅通」
- **Key 为空**：弹 toast 提示「请先输入 API Key」

### 技术方案

| 模块 | 选型 | 说明 |
|------|------|------|
| API 调用 | Rust `reqwest` | 后端发起 HTTPS 请求，避免前端暴露 Key |
| 验证接口 | `POST https://api.deepseek.com/v1/chat/completions` | 发送最小对话，检查 HTTP 200 |
| 存储方式 | `tauri-plugin-store` | Key 落盘加密存储，重启保留 |
| 模型配置 | 默认 `deepseek v4pro` | 写入 store，后续功能读取 |

### 后续用途（依赖本功能）

- 🧠 **智能盯盘解析**：用 LLM 替代正则，理解更复杂的自然语言（如「茅台如果今天先跌3%再拉回来就告诉我」）
- 💬 **角色对话**：Live2D 角色接入 AI 对话能力
- 📊 **行情解读**：让 AI 对盯盘触发事件做一句话解读

### 代码位置（规划）

- **前端**：`src/components/ApiKeyPanel.tsx`（设置面板）
- **后端**：`src-tauri/src/deepseek.rs`（API 验证、调用封装）
- **存储**：Store 插件管理 `deepseek-config.json`
